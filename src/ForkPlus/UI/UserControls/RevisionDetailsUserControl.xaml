<UserControl xmlns:userControls="clr-namespace:ForkPlus.UI.UserControls" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="ForkPlus.UI.UserControls.RevisionDetailsUserControl" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:controls="clr-namespace:ForkPlus.UI.Controls">
  <controls:ContentContainer xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Name="ContentContainer">
    <Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Grid Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}" SizeChanged="TabControl_SizeChanged" x:Name="TabHeaderContainer">
        <Grid.RowDefinitions>
          <RowDefinition Height="30" />
          <RowDefinition Height="1.5" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="40" />
        </Grid.ColumnDefinitions>
        <RadioButton Grid.Column="0" Grid.Row="0" Content="Commit" Style="{StaticResource RevisionDetailsBarButton}" FontWeight="Medium" GroupName="RevisionDetailsTabs" Checked="TabRadioButton_Checked" x:Name="CommitRadioButton" />
        <RadioButton Grid.Column="1" Grid.Row="0" Content="Changes" Style="{StaticResource RevisionDetailsBarButton}" FontWeight="Medium" GroupName="RevisionDetailsTabs" Checked="TabRadioButton_Checked" x:Name="ChangesRadioButton" />
        <RadioButton Grid.Column="2" Grid.Row="0" Content="File Tree" Style="{StaticResource RevisionDetailsBarButton}" FontWeight="Medium" GroupName="RevisionDetailsTabs" Checked="TabRadioButton_Checked" x:Name="FileTreeRadioButton" />
        <Canvas Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
          <Border Height="1.5" Margin="0" BorderThickness="0" HorizontalAlignment="Left" Background="{DynamicResource SystemAccentBrush}" Canvas.Left="0" x:Name="IndicatorBorder" />
        </Canvas>
        <Button Grid.Column="4" HorizontalAlignment="Right" VerticalAlignment="Center" BorderThickness="0" Background="#00FFFFFF" Width="22" Height="22" Padding="2" Margin="0,1.5,10,0" ToolTip="Open in separate window" Style="{DynamicResource RevisionDetailsSwapButtonStyle}" x:Name="ShowRevisionInSeparateWindowButton">
          <Image Source="{DynamicResource OpenInSeparateWindowIcon}" Height="16" Width="16" />
        </Button>
      </Grid>
      <userControls:RevisionsHeaderUserControl Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" x:Name="RevisionDetailsHeaderUserControl" />
      <Border Grid.Row="2" Padding="0" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}" UseLayoutRounding="True">
        <Grid>
          <userControls:RevisionSummaryUserControl x:Name="SummaryUserControl" />
          <userControls:RevisionChangesUserControl Visibility="Collapsed" x:Name="ChangesUserControl" />
          <userControls:RevisionFileTreeUserControl Visibility="Collapsed" x:Name="FileTreeUserControl" />
        </Grid>
      </Border>
    </Grid>
  </controls:ContentContainer>
</UserControl>