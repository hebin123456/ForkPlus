<!-- Migrated from WPF to Avalonia: root xmlns updated to https://github.com/avaloniaui; redundant child xmlns declarations removed. -->
<UserControl xmlns:UserControls="clr-namespace:ForkPlus.UI.UserControls" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="ForkPlus.UI.UserControls.UserColorsUserControl" xmlns="https://github.com/avaloniaui">
  <Border Style="{DynamicResource MenuShadowBorderStyle}">
    <Border BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Background="{DynamicResource BackgroundBrush}" CornerRadius="6">
      <ItemsControl Margin="4" x:Name="Colors">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Rows="4" Columns="12" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Grid Margin="1">
              <ToggleButton Style="{DynamicResource UserColorToggleButton}" Checked="ColorButton_Changed" Unchecked="ColorButton_Changed" x:Name="UserColorRadioButton">
                <ToggleButton.Background>
                  <Binding Path="Brush" />
                </ToggleButton.Background>
                <ToggleButton.IsChecked>
                  <Binding Path="IsSelected" />
                </ToggleButton.IsChecked>
              </ToggleButton>
            </Grid>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Border>
  </Border>
</UserControl>
