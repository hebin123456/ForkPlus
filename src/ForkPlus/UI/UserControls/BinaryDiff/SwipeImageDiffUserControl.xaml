<!-- 已从 WPF 迁移到 Avalonia：xmlns 替换为 https://github.com/avaloniaui；删除 ResourceDictionary / Style / 子控件上的重复 xmlns 与 xmlns:x 声明；Label 替换为 TextBlock（含 <Label.Style> → <TextBlock.Style>）。 -->
<UserControl xmlns:binaryDiff="clr-namespace:ForkPlus.UI.UserControls.BinaryDiff" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="ForkPlus.UI.UserControls.BinaryDiff.SwipeImageDiffUserControl" xmlns="https://github.com/avaloniaui">
  <UserControl.Resources>
    <ResourceDictionary>
      <Style x:Key="SwipeGridSplitter" TargetType="{x:Type GridSplitter}">
        <Setter Property="FrameworkElement.FocusVisualStyle">
          <Setter.Value>
            <x:Null />
          </Setter.Value>
        </Setter>
        <Setter Property="KeyboardNavigation.IsTabStop" Value="False" />
        <Setter Property="FrameworkElement.VerticalAlignment" Value="Stretch" />
        <Setter Property="FrameworkElement.HorizontalAlignment" Value="Center" />
        <Setter Property="FrameworkElement.Margin" Value="-3,0" />
        <Setter Property="FrameworkElement.Width" Value="6" />
        <Setter Property="Panel.Background" Value="{DynamicResource AccentSecondaryBrush}" />
        <Setter Property="Control.Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type GridSplitter}">
              <Grid Background="#00FFFFFF">
                <Grid.RowDefinitions>
                  <RowDefinition Height="9" />
                  <RowDefinition Height="*" />
                  <RowDefinition Height="9" />
                </Grid.RowDefinitions>
                <Border CornerRadius="3" Width="6" Height="6" Margin="0,3,0,0" HorizontalAlignment="Center" Background="{TemplateBinding Panel.Background}" />
                <Border Grid.Row="1" BorderBrush="{TemplateBinding Border.BorderBrush}" Width="2" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" />
                <Border Grid.Row="2" CornerRadius="3" Width="6" Height="6" Margin="0,0,0,3" HorizontalAlignment="Center" Background="{TemplateBinding Panel.Background}" />
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </ResourceDictionary>
  </UserControl.Resources>
  <Grid Margin="0,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Top">
    <Grid.RowDefinitions>
      <RowDefinition Height="35" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <DockPanel LastChildFill="False">
      <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" HorizontalAlignment="Left">
        <Grid HorizontalAlignment="Left">
          <TextBlock Margin="0,10,0,6" Style="{DynamicResource LfsLabel}" x:Name="OldLfsLabel" />
          <TextBlock Margin="0,10,0,6" Style="{DynamicResource NotLfsLabel}" x:Name="OldNotLfsLabel" />
        </Grid>
        <TextBlock Margin="4,10,0,6" FontSize="13" FontWeight="Medium" Text="old" HorizontalAlignment="Right" Foreground="{DynamicResource Diff.Removed.Foreground}" x:Name="OldTextBlock" />
      </StackPanel>
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
        <Grid HorizontalAlignment="Right">
          <TextBlock Margin="0,10,0,6" Style="{DynamicResource LfsLabel}" x:Name="NewLfsLabel" />
          <TextBlock Margin="0,10,0,6" Style="{DynamicResource NotLfsLabel}" x:Name="NewNotLfsLabel" />
        </Grid>
        <TextBlock Margin="4,10,0,6" FontSize="13" FontWeight="Medium" Text="new" HorizontalAlignment="Right" Foreground="{DynamicResource Diff.Added.Foreground}" x:Name="NewTextBlock" />
      </StackPanel>
    </DockPanel>
    <Border Grid.Row="1" Style="{DynamicResource BinaryDiffShadowBorder}">
      <Grid Background="{DynamicResource BinaryDiffBackground}">
        <Border Margin="8" BorderThickness="1" BorderBrush="{DynamicResource BinaryDiffBorder}" Background="{DynamicResource TransparentVisualBrush}">
          <binaryDiff:OverlayImageControl x:Name="OverlayImage" />
        </Border>
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="9" />
            <RowDefinition Height="*" />
            <RowDefinition Height="9" />
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="9" />
          </Grid.ColumnDefinitions>
          <Grid Grid.Column="1" SizeChanged="ClipXPlaceholderGrid_SizeChanged" x:Name="ClipXPlaceholderGrid" />
          <GridSplitter Grid.Row="0" Grid.RowSpan="3" Grid.Column="2" Style="{DynamicResource SwipeGridSplitter}" ResizeBehavior="PreviousAndNext" x:Name="GridSplitter" />
        </Grid>
      </Grid>
    </Border>
  </Grid>
</UserControl>
