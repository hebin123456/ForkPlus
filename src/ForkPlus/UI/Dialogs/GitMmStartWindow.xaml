<dialogs:ForkPlusDialogWindow Width="490" SizeToContent="Height" xmlns="https://github.com/avaloniaui" xmlns:dialogs="clr-namespace:ForkPlus.UI.Dialogs" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="ForkPlus.UI.Dialogs.GitMmStartWindow" xmlns:controls="clr-namespace:ForkPlus.UI.Controls">
  <Grid>
    
<Grid.Resources><ResourceDictionary>
<Style x:Key="GitMmAdvancedOptionsExpanderStyle" Selector="Expander">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Expander">
              <DockPanel>
                <ToggleButton DockPanel.Dock="Top" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" Content="{TemplateBinding Header}" Padding="6,3" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Background="{DynamicResource BackgroundBrush}" HorizontalAlignment="Left">
                  <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                      <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="0">
                        <DockPanel Margin="{TemplateBinding Padding}">
                          <!-- 阶段 4.5：原WPF ControlTemplate.Triggers 根据 IsChecked 切换 ArrowText ">" / "v"，
                               用两个堆叠 TextBlock + IsVisible 绑定 IsChecked 替代。 -->
                          <Grid Width="12">
                            <TextBlock Text=">" VerticalAlignment="Center" IsVisible="{Binding !IsChecked, RelativeSource={RelativeSource TemplatedParent}}" />
                            <TextBlock Text="v" VerticalAlignment="Center" IsVisible="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}}" />
                          </Grid>
                          <ContentPresenter Margin="4,0,0,0" VerticalAlignment="Center" />
                        </DockPanel>
                      </Border>
                    </ControlTemplate>
                  </ToggleButton.Template>
                </ToggleButton>
                <!-- 阶段 4.5：原WPF ControlTemplate.Triggers 根据 IsExpanded 切换 Visibility，已改用 IsVisible Binding 替代 -->
                <ContentPresenter DockPanel.Dock="Bottom" IsVisible="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" x:Name="ExpandSite" />
              </DockPanel>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
</ResourceDictionary></Grid.Resources>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="80" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid Grid.Row="1" Grid.Column="1" Margin="0,10,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <TextBlock Grid.Column="0" Grid.Row="0" Text="Branch name:" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" />
      <controls:PlaceholderTextBox TextChanged="BranchNameTextBox_TextChanged" Grid.Row="0" Grid.Column="1" Margin="8,4,0,4" Padding="4,2,4,2" Placeholder="Branch name" x:Name="BranchNameTextBox" />
      <TextBlock Grid.Column="0" Grid.Row="1" Text="Sub repositories:" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" />
      <Button Grid.Column="1" Grid.Row="1" Margin="8,4,0,4" HorizontalAlignment="Stretch"  Click="SubreposDropDownButton_Click" x:Name="SubreposDropDownButton" />
      <Expander Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Margin="0,6,0,0" Header="Advanced Options" IsExpanded="False">
        <Grid Margin="0,4,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock Grid.Column="0" Grid.Row="0" Text="Jobs:" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" />
          <ComboBox Grid.Column="1" Grid.Row="0" Margin="8,4,0,4" Width="70" HorizontalAlignment="Left" x:Name="JobsComboBox">
            <ComboBoxItem Content="1" />
            <ComboBoxItem Content="2" />
            <ComboBoxItem Content="3" />
            <ComboBoxItem Content="4" />
            <ComboBoxItem Content="5" />
            <ComboBoxItem Content="6" />
            <ComboBoxItem Content="7" />
            <ComboBoxItem Content="8" />
            <ComboBoxItem Content="9" />
            <ComboBoxItem Content="10" />
          </ComboBox>
          <TextBlock Grid.Column="0" Grid.Row="1" Text="Search mode:" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" />
          <ComboBox Grid.Column="1" Grid.Row="1" Margin="8,4,0,4" Width="120" HorizontalAlignment="Left" x:Name="GrepModeComboBox">
            <ComboBoxItem Content="mixed" />
            <ComboBoxItem Content="name" />
            <ComboBoxItem Content="path" />
            <ComboBoxItem Content="namereg" />
            <ComboBoxItem Content="pathreg" />
            <ComboBoxItem Content="mixedreg" />
            <ComboBoxItem Content="underpath" />
          </ComboBox>
          <CheckBox Grid.Column="1" Grid.Row="2" Margin="8,4,0,0" HorizontalAlignment="Left" Content="_All sub repositories" IsChecked="True" Checked="AllSubreposCheckBox_Changed" Unchecked="AllSubreposCheckBox_Changed" x:Name="AllSubreposCheckBox" />
          <Grid Grid.Column="1" Grid.Row="3" Margin="8,4,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="180" />
              <ColumnDefinition Width="180" />
            </Grid.ColumnDefinitions>
            <CheckBox Grid.Column="0" HorizontalAlignment="Left" Content="Allow tag" ToolTip.Tip="--allow-tag" x:Name="AllowTagCheckBox" />
            <CheckBox Grid.Column="1" HorizontalAlignment="Left" Content="Allow commit" ToolTip.Tip="--allow-commit" x:Name="AllowCommitCheckBox" />
          </Grid>
          <Grid Grid.Column="1" Grid.Row="4" Margin="8,4,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="180" />
              <ColumnDefinition Width="180" />
            </Grid.ColumnDefinitions>
            <CheckBox Grid.Column="0" HorizontalAlignment="Left" Content="Allow no tracking branch" ToolTip.Tip="--allow-no-track" x:Name="AllowNoTrackCheckBox" />
            <CheckBox Grid.Column="1" HorizontalAlignment="Left" Content="Start from HEAD" ToolTip.Tip="--head" x:Name="HeadCheckBox" />
          </Grid>
        </Grid>
      </Expander>
      <TextBlock Grid.Column="0" Grid.Row="8" Text="Command Preview:" FontSize="13" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,8,0,0" />
      <ScrollViewer Grid.Column="1" Grid.Row="8" MaxHeight="120" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="8,8,0,0">
        <TextBlock FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource SecondaryLabelBrush}" x:Name="CommandPreviewTextBlock" />
      </ScrollViewer>
    </Grid>
  </Grid>
</dialogs:ForkPlusDialogWindow>
