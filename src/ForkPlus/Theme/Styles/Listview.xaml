<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:ui="clr-namespace:ForkPlus.UI" xmlns:controls="clr-namespace:ForkPlus.UI.Controls" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" />
  <Style x:Key="ListItemRoundedBorderStyle" TargetType="{x:Type Border}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.Margin" Value="4,0,4,0" />
    <Setter Property="Border.CornerRadius" Value="4" />
  </Style>
  <Style x:Key="ListItemNotRoundedBorderStyle" TargetType="{x:Type Border}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.Margin" Value="4,0,4,0" />
    <Setter Property="Border.CornerRadius" Value="0" />
  </Style>
  <DataTemplate x:Key="ReferencesDataTemplates" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate.Resources>
      <ResourceDictionary>
        <DataTemplate DataType="{x:Type ui:TagViewModel}" x:Key="{DataTemplateKey {x:Type ui:TagViewModel}}">
          <StackPanel Orientation="Horizontal" Height="18" Margin="3,2,3,2">
            <Border BorderThickness="1" CornerRadius="4,0,0,4" BorderBrush="{DynamicResource RevisionList.TagBorder}" Background="{DynamicResource ListBox.Static.Background}">
              <Image Source="{DynamicResource TagIcon}" Height="14" Width="14" Margin="2,0,2,0" />
            </Border>
            <Border BorderThickness="0,1,1,1" Padding="2,0" CornerRadius="0,4,4,0" BorderBrush="{DynamicResource RevisionList.TagBorder}" Background="{DynamicResource RevisionList.TagBackground}">
              <controls:HighlightableTextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                <controls:HighlightableTextBlock.Text>
                  <Binding Path="Name" />
                </controls:HighlightableTextBlock.Text>
                <controls:HighlightableTextBlock.HighlightString>
                  <Binding Path="SearchString" />
                </controls:HighlightableTextBlock.HighlightString>
              </controls:HighlightableTextBlock>
            </Border>
          </StackPanel>
        </DataTemplate>
        <DataTemplate DataType="{x:Type ui:LocalBranchViewModel}" x:Key="{DataTemplateKey {x:Type ui:LocalBranchViewModel}}">
          <Border Height="18" BorderThickness="1" CornerRadius="4" Margin="3,2,3,2">
            <Border.BorderBrush>
              <Binding Path="BorderBrush" />
            </Border.BorderBrush>
            <Border.Background>
              <Binding Path="BackgroundBrush" />
            </Border.Background>
            <StackPanel Orientation="Horizontal">
              <Path Stretch="None" Margin="3,0,0,0" VerticalAlignment="Center" Data="{DynamicResource OptionMarkGeomentry}" Fill="{DynamicResource ReferenceForeground}" Visibility="Collapsed" x:Name="ActiveBranchIcon" />
              <Border BorderThickness="0" CornerRadius="4,0,0,4" Visibility="Collapsed" x:Name="IconBorder">
                <Border.BorderBrush>
                  <Binding Path="BorderBrush" />
                </Border.BorderBrush>
                <Border.Background>
                  <Binding Path="BackgroundBrush" />
                </Border.Background>
                <Image Source="{DynamicResource WorktreeIcon}" Height="14" Width="14" Margin="2,0,2,0" />
              </Border>
              <controls:HighlightableTextBlock FontWeight="Regular" Margin="4,-1,4,1" Style="{DynamicResource ReferenceLabelTextBlockStyle}" x:Name="NameTextBlock">
                <controls:HighlightableTextBlock.Text>
                  <Binding Path="Name" />
                </controls:HighlightableTextBlock.Text>
                <controls:HighlightableTextBlock.HighlightString>
                  <Binding Path="SearchString" />
                </controls:HighlightableTextBlock.HighlightString>
              </controls:HighlightableTextBlock>
            </StackPanel>
          </Border>
          <DataTemplate.Triggers>
            <DataTrigger Value="true">
              <DataTrigger.Binding>
                <Binding Path="IsActive" />
              </DataTrigger.Binding>
              <Setter TargetName="NameTextBlock" Property="TextElement.FontWeight" Value="Bold" />
              <Setter TargetName="ActiveBranchIcon" Property="UIElement.Visibility" Value="Visible" />
            </DataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Value="true">
                  <Condition.Binding>
                    <Binding Path="IsActive" />
                  </Condition.Binding>
                </Condition>
                <Condition Value="true">
                  <Condition.Binding>
                    <Binding Path="IsWorktree" />
                  </Condition.Binding>
                </Condition>
              </MultiDataTrigger.Conditions>
              <Setter TargetName="IconBorder" Property="UIElement.Visibility" Value="Visible" />
              <Setter TargetName="IconBorder" Property="Border.BorderThickness" Value="0,0,1,0" />
              <Setter TargetName="IconBorder" Value="{DynamicResource ListBox.Static.Background}" Property="Panel.Background" />
            </MultiDataTrigger>
          </DataTemplate.Triggers>
        </DataTemplate>
        <DataTemplate DataType="{x:Type ui:RemoteBranchViewModel}" x:Key="{DataTemplateKey {x:Type ui:RemoteBranchViewModel}}">
          <StackPanel Orientation="Horizontal" Height="18" Margin="3,2,3,2" x:Name="Container">
            <Border BorderThickness="1" CornerRadius="4" Background="{DynamicResource ListBox.Static.Background}" x:Name="Border">
              <Border.BorderBrush>
                <Binding Path="BorderBrush" />
              </Border.BorderBrush>
              <Image Height="14" Width="14" Margin="2,0,2,0" x:Name="Icon">
                <Image.Source>
                  <Binding Path="RemoteIcon" />
                </Image.Source>
              </Image>
            </Border>
            <Border BorderThickness="0,1,1,1" Padding="2,0" CornerRadius="0,4,4,0" x:Name="NameContainer">
              <Border.BorderBrush>
                <Binding Path="BorderBrush" />
              </Border.BorderBrush>
              <Border.Background>
                <Binding Path="BackgroundBrush" />
              </Border.Background>
              <controls:HighlightableTextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                <controls:HighlightableTextBlock.Text>
                  <Binding Path="Name" />
                </controls:HighlightableTextBlock.Text>
                <controls:HighlightableTextBlock.HighlightString>
                  <Binding Path="SearchString" />
                </controls:HighlightableTextBlock.HighlightString>
              </controls:HighlightableTextBlock>
            </Border>
          </StackPanel>
          <DataTemplate.Triggers>
            <DataTrigger Value="true">
              <DataTrigger.Binding>
                <Binding Path="HasDownstream" />
              </DataTrigger.Binding>
              <Setter TargetName="NameContainer" Property="UIElement.Visibility" Value="Collapsed" />
              <Setter TargetName="Border" Property="Border.CornerRadius" Value="4" />
              <Setter TargetName="Container" Property="FrameworkElement.Margin" Value="3,2,1,2" />
            </DataTrigger>
            <DataTrigger Value="false">
              <DataTrigger.Binding>
                <Binding Path="HasDownstream" />
              </DataTrigger.Binding>
              <Setter TargetName="NameContainer" Property="UIElement.Visibility" Value="Visible" />
              <Setter TargetName="Border" Property="Border.CornerRadius" Value="4,0,0,4" />
            </DataTrigger>
          </DataTemplate.Triggers>
        </DataTemplate>
        <DataTemplate DataType="{x:Type ui:BisectMarkViewModel}" x:Key="{DataTemplateKey {x:Type ui:BisectMarkViewModel}}">
          <StackPanel Orientation="Horizontal" Height="18" Margin="3,2,3,2">
            <Border BorderThickness="1" CornerRadius="4,0,0,4" BorderBrush="{DynamicResource RevisionList.BisectBadBorder}" Background="{DynamicResource ListBox.Static.Background}" x:Name="IconBorder">
              <Image Height="14" Width="14" Margin="2,0,2,0" Source="{DynamicResource BisectBadIcon}" x:Name="Icon" />
            </Border>
            <Border BorderThickness="0,1,1,1" Padding="2,0" CornerRadius="0,4,4,0" BorderBrush="{DynamicResource RevisionList.BisectBadBorder}" Background="{DynamicResource RevisionList.BisectBadBackground}" x:Name="LabelBorder">
              <TextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                <TextBlock.Text>
                  <Binding Path="Name" />
                </TextBlock.Text>
              </TextBlock>
            </Border>
          </StackPanel>
          <DataTemplate.Triggers>
            <DataTrigger Value="true">
              <DataTrigger.Binding>
                <Binding Path="IsGood" />
              </DataTrigger.Binding>
              <Setter TargetName="Icon" Value="{DynamicResource BisectGoodIcon}" Property="Image.Source" />
              <Setter TargetName="IconBorder" Value="{DynamicResource RevisionList.BisectGoodBorder}" Property="Border.BorderBrush" />
              <Setter TargetName="LabelBorder" Value="{DynamicResource RevisionList.BisectGoodBorder}" Property="Border.BorderBrush" />
              <Setter TargetName="LabelBorder" Value="{DynamicResource RevisionList.BisectGoodBackground}" Property="Panel.Background" />
            </DataTrigger>
          </DataTemplate.Triggers>
        </DataTemplate>
        <DataTemplate DataType="{x:Type ui:StashReferenceViewModel}" x:Key="{DataTemplateKey {x:Type ui:StashReferenceViewModel}}">
          <Border Height="18" BorderThickness="1" CornerRadius="4" Padding="0" Margin="3,2,3,2" BorderBrush="{DynamicResource RevisionList.StashBorder}" Background="{DynamicResource ListBox.Static.Background}">
            <DockPanel>
              <Image Source="{DynamicResource SidebarStashIcon}" DockPanel.Dock="Left" Height="14" Width="14" Margin="2,0,2,0" />
              <Border BorderThickness="1,0,0,0" Padding="2,0" CornerRadius="0,3,3,0" BorderBrush="{DynamicResource RevisionList.StashBorder}" Background="{DynamicResource RevisionList.StashBackground}">
                <TextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                  <TextBlock.Text>
                    <Binding Path="ReflogName" />
                  </TextBlock.Text>
                </TextBlock>
              </Border>
            </DockPanel>
          </Border>
        </DataTemplate>
      </ResourceDictionary>
    </DataTemplate.Resources>
    <ContentPresenter>
      <ContentPresenter.Content>
        <Binding />
      </ContentPresenter.Content>
    </ContentPresenter>
  </DataTemplate>
  <Style x:Key="FocusVisual" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource Item.SelectedActive.Background}" StrokeThickness="1" StrokeDashArray="1 2" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ListViewItemStyle" TargetType="{x:Type ListViewItem}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="UIElement.SnapsToDevicePixels" Value="True" />
    <Setter Property="Control.Padding" Value="4,1" />
    <Setter Property="Control.HorizontalContentAlignment">
      <Setter.Value>
        <Binding Path="HorizontalContentAlignment"><Binding.RelativeSource><RelativeSource AncestorType="{x:Type ItemsControl}" /></Binding.RelativeSource></Binding>
      </Setter.Value>
    </Setter>
    <Setter Property="Control.VerticalContentAlignment">
      <Setter.Value>
        <Binding Path="VerticalContentAlignment"><Binding.RelativeSource><RelativeSource AncestorType="{x:Type ItemsControl}" /></Binding.RelativeSource></Binding>
      </Setter.Value>
    </Setter>
    <Setter Property="Panel.Background" Value="{DynamicResource ListBox.Static.Background}" />
    <Setter Property="Border.BorderBrush" Value="#00FFFFFF" />
    <Setter Property="Border.BorderThickness" Value="1" />
    <Setter Property="FrameworkElement.FocusVisualStyle" Value="{DynamicResource FocusVisual}" />
    <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListViewItem}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" Style="{DynamicResource ListItemRoundedBorderStyle}" x:Name="Bd">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="UIElement.IsMouseOver" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.MouseOver.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.MouseOver.Border}" Property="Border.BorderBrush" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="False" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Border}" Property="Border.BorderBrush" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="True" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Border}" Property="Border.BorderBrush" />
              <Setter TargetName="Bd" Value="{DynamicResource ForegroundBrush.Light}" Property="TextElement.Foreground" />
            </MultiTrigger>
            <Trigger Property="UIElement.IsEnabled" Value="False">
              <Setter TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Property="TextElement.Foreground" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type ListView}" TargetType="{x:Type ListView}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Panel.Background" Value="{DynamicResource ListBox.Static.Background}" />
    <Setter Property="Border.BorderBrush" Value="{DynamicResource ListBox.Static.Border}" />
    <Setter Property="Border.BorderThickness" Value="1" />
    <Setter Property="Control.Padding" Value="0,4,0,0" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource ForegroundBrush}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
    <Setter Property="ScrollViewer.PanningMode" Value="Both" />
    <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
    <Setter Property="Control.VerticalContentAlignment" Value="Center" />
    <Setter Property="ItemsControl.ItemContainerStyle" Value="{DynamicResource ListViewItemStyle}" />
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListView}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" SnapsToDevicePixels="true" x:Name="Bd">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Control.Padding}">
              <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
            </ScrollViewer>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="GridViewColumnHeaderStyle" TargetType="{x:Type GridViewColumnHeader}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="UIElement.Visibility" Value="Collapsed" />
  </Style>
  <Style x:Key="ListViewWithGridViewScrollViewerStyle" TargetType="{x:Type ScrollViewer}" BasedOn="{StaticResource {x:Type ScrollViewer}}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Panel.Background" Value="{DynamicResource ListBox.Static.Background}" />
    <Setter Property="UIElement.Focusable" Value="False" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollViewer}">
          <Grid Background="{TemplateBinding Panel.Background}" SnapsToDevicePixels="True">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DockPanel Margin="{TemplateBinding Control.Padding}">
              <controls:TouchpadAwareScrollViewer DockPanel.Dock="Top" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
                <GridViewHeaderRowPresenter Margin="0" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" Columns="{Binding Path=TemplatedParent.View.Columns, RelativeSource={RelativeSource TemplatedParent}}">
                  <GridViewHeaderRowPresenter.AllowsColumnReorder>
                    <Binding Path="TemplatedParent.View.AllowsColumnReorder"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
                  </GridViewHeaderRowPresenter.AllowsColumnReorder>
                  <GridViewHeaderRowPresenter.ColumnHeaderContainerStyle>
                    <Binding Path="TemplatedParent.View.ColumnHeaderContainerStyle"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
                  </GridViewHeaderRowPresenter.ColumnHeaderContainerStyle>
                  <GridViewHeaderRowPresenter.ColumnHeaderToolTip>
                    <Binding Path="TemplatedParent.View.ColumnHeaderToolTip"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
                  </GridViewHeaderRowPresenter.ColumnHeaderToolTip>
                  <GridViewHeaderRowPresenter.ColumnHeaderContextMenu>
                    <Binding Path="TemplatedParent.View.ColumnHeaderContextMenu"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
                  </GridViewHeaderRowPresenter.ColumnHeaderContextMenu>
                  <GridViewHeaderRowPresenter.ColumnHeaderTemplate>
                    <Binding Path="TemplatedParent.View.ColumnHeaderTemplate"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
                  </GridViewHeaderRowPresenter.ColumnHeaderTemplate>
                </GridViewHeaderRowPresenter>
              </controls:TouchpadAwareScrollViewer>
              <ScrollContentPresenter CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False" Content="{TemplateBinding ContentControl.Content}" ContentTemplate="{x:Null}" KeyboardNavigation.DirectionalNavigation="Local" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" x:Name="PART_ScrollContentPresenter" />
            </DockPanel>
            <ScrollBar Cursor="Arrow" Maximum="{TemplateBinding ScrollViewer.ScrollableWidth}" Minimum="0" Orientation="Horizontal" Grid.Row="1" Visibility="{TemplateBinding ScrollViewer.ComputedHorizontalScrollBarVisibility}" ViewportSize="{TemplateBinding ScrollViewer.ViewportWidth}" Style="{DynamicResource HorizontalScrollBarStyle}" x:Name="PART_HorizontalScrollBar">
              <ScrollBar.Value>
                <Binding Path="HorizontalOffset" Mode="OneWay"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
              </ScrollBar.Value>
            </ScrollBar>
            <ScrollBar Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollViewer.ScrollableHeight}" Minimum="0" Orientation="Vertical" Visibility="{TemplateBinding ScrollViewer.ComputedVerticalScrollBarVisibility}" ViewportSize="{TemplateBinding ScrollViewer.ViewportHeight}" Style="{DynamicResource VerticalScrollBarStyle}" x:Name="PART_VerticalScrollBar">
              <ScrollBar.Value>
                <Binding Path="VerticalOffset" Mode="OneWay"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
              </ScrollBar.Value>
            </ScrollBar>
            <DockPanel Background="{DynamicResource ScrollViewerBackground}" Grid.Column="1" LastChildFill="False" Grid.Row="1">
              <Rectangle DockPanel.Dock="Left" Fill="{DynamicResource ScrollBar.Static.Background}" Width="1" Visibility="{TemplateBinding ScrollViewer.ComputedVerticalScrollBarVisibility}" />
              <Rectangle DockPanel.Dock="Top" Fill="{DynamicResource ScrollBar.Static.Background}" Height="1" Visibility="{TemplateBinding ScrollViewer.ComputedHorizontalScrollBarVisibility}" />
            </DockPanel>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ListViewItemGridViewStyle" TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemStyle}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="TextElement.FontWeight" Value="Normal" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListViewItem}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="0" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" Margin="4,0,4,0" x:Name="Bd">
            <Border.CornerRadius>
              <Binding Path="SelectionType"><Binding.Converter><ui:SelectionToCornerRadiusConverter /></Binding.Converter></Binding>
            </Border.CornerRadius>
            <GridViewRowPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="False" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Border}" Property="Border.BorderBrush" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="True" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Border}" Property="Border.BorderBrush" />
              <Setter TargetName="Bd" Value="{DynamicResource ForegroundBrush.Light}" Property="TextElement.Foreground" />
              <Setter Value="{DynamicResource ForegroundBrush.Light}" Property="TextElement.Foreground" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ListViewWithGridViewStyle" TargetType="{x:Type ListView}" BasedOn="{StaticResource {x:Type ListView}}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style.Resources>
      <ResourceDictionary>
        <Style x:Key="{x:Type ListViewItem}" TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemGridViewStyle}" />
      </ResourceDictionary>
    </Style.Resources>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListView}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="0" Background="{TemplateBinding Panel.Background}" Padding="0" SnapsToDevicePixels="true" x:Name="Bd">
            <controls:TouchpadAwareScrollViewer Focusable="false" Padding="{TemplateBinding Control.Padding}" Style="{DynamicResource ListViewWithGridViewScrollViewerStyle}">
              <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
            </controls:TouchpadAwareScrollViewer>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="NoSelectionRevisionListViewItemStyle" TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemGridViewStyle}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="UIElement.Focusable" Value="False" />
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="Control.HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Control.VerticalContentAlignment" Value="Stretch" />
    <Setter Property="Control.Padding" Value="0" />
    <Setter Property="FrameworkElement.Margin" Value="0" />
    <Style.Triggers>
      <DataTrigger>
        <DataTrigger.Binding>
          <Binding Path="IsHead" />
        </DataTrigger.Binding>
        <Setter Property="TextElement.FontWeight" Value="Bold" />
      </DataTrigger>
      <DataTrigger>
        <DataTrigger.Binding>
          <Binding Path="IsReachable" />
        </DataTrigger.Binding>
        <Setter Property="TextElement.Foreground" Value="{DynamicResource RevisionList.Unreachable.ForegroundBrush}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="ListBoxItemStyle" TargetType="{x:Type ListBoxItem}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="Border.BorderThickness" Value="0" />
    <Setter Property="Panel.Background" Value="{DynamicResource Item.Static.Background}" />
    <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" Style="{DynamicResource ListItemRoundedBorderStyle}" x:Name="Bd">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="False" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Border}" Property="Border.BorderBrush" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="True" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Border}" Property="Border.BorderBrush" />
              <Setter TargetName="Bd" Value="{DynamicResource ForegroundBrush.Light}" Property="TextElement.Foreground" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="MultiselectListBoxItemStyle" TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource ListBoxItemStyle}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" Margin="4,0,4,0" x:Name="Bd">
            <Border.CornerRadius>
              <Binding Path="SelectionType"><Binding.Converter><ui:SelectionToCornerRadiusConverter /></Binding.Converter></Binding>
            </Border.CornerRadius>
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="False" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Border}" Property="Border.BorderBrush" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="True" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Border}" Property="Border.BorderBrush" />
              <Setter TargetName="Bd" Value="{DynamicResource ForegroundBrush.Light}" Property="TextElement.Foreground" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="NoSelectionListBoxItemStyle" TargetType="{x:Type ListBoxItem}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="UIElement.Focusable" Value="False" />
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="Border.BorderThickness" Value="0" />
    <Setter Property="Panel.Background" Value="{DynamicResource Item.Static.Background}" />
    <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" x:Name="Bd">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="NoVisibleSelectionListBoxItemStyle" TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource NoSelectionListBoxItemStyle}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="UIElement.Focusable" Value="True" />
  </Style>
  <Style x:Key="ListBoxItemWithBorderStyle" TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource ListBoxItemStyle}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Border.BorderBrush" Value="{DynamicResource BorderBrush}" />
    <Setter Property="Border.BorderThickness" Value="0,0,0,1" />
    <Setter Property="Panel.Background" Value="{DynamicResource Item.Static.Background}" />
    <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" Style="{DynamicResource ListItemNotRoundedBorderStyle}" x:Name="Bd">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="False" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource ListItemRoundedBorderStyle}" Property="FrameworkElement.Style" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Border}" Property="Border.BorderBrush" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelectionActive" Value="True" />
                <Condition Property="ListBoxItem.IsSelected" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource ListItemRoundedBorderStyle}" Property="FrameworkElement.Style" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Border}" Property="Border.BorderBrush" />
              <Setter TargetName="Bd" Value="{DynamicResource ForegroundBrush.Light}" Property="TextElement.Foreground" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type ListBox}" TargetType="{x:Type ListBox}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Panel.Background" Value="{DynamicResource ListBox.Static.Background}" />
    <Setter Property="Border.BorderBrush" Value="{DynamicResource ListBox.Static.Border}" />
    <Setter Property="Border.BorderThickness" Value="1" />
    <Setter Property="Control.Padding" Value="0,4,0,0" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource ForegroundBrush}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
    <Setter Property="ScrollViewer.PanningMode" Value="Both" />
    <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
    <Setter Property="Control.VerticalContentAlignment" Value="Center" />
    <Setter Property="ItemsControl.ItemContainerStyle" Value="{DynamicResource ListBoxItemStyle}" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBox}">
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="0" SnapsToDevicePixels="true" x:Name="Bd">
            <controls:TouchpadAwareScrollViewer Focusable="false" Padding="{TemplateBinding Control.Padding}" Background="{TemplateBinding Panel.Background}">
              <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
            </controls:TouchpadAwareScrollViewer>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="ListBox.SelectionMode" Value="Extended">
        <Setter Property="ItemsControl.ItemContainerStyle" Value="{DynamicResource MultiselectListBoxItemStyle}" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="NoSelectionListBoxStyle" TargetType="{x:Type ListBox}" BasedOn="{StaticResource {x:Type ListBox}}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="ItemsControl.ItemContainerStyle" Value="{DynamicResource NoSelectionListBoxItemStyle}" />
  </Style>
  <Style x:Key="AutoCompleteListBoxStyle" TargetType="{x:Type ListBox}" BasedOn="{StaticResource {x:Type ListBox}}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.VerticalAlignment" Value="Top" />
    <Setter Property="FrameworkElement.HorizontalAlignment" Value="Left" />
    <Setter Property="FrameworkElement.MinWidth" Value="196" />
    <Setter Property="FrameworkElement.Height" Value="78" />
    <Setter Property="Control.Padding" Value="0,2,0,0" />
    <Setter Property="Border.BorderThickness" Value="1" />
    <Setter Property="Border.BorderBrush" Value="{DynamicResource Menu.MenuItem.Static.Border}" />
    <Setter Property="Panel.Background" Value="{DynamicResource Menu.MenuItem.Static.Background}" />
    <Setter Property="ItemsControl.ItemContainerStyle" Value="{DynamicResource AutocompleteListBoxItemStyle}" />
    <Setter Property="Control.HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Control.VerticalContentAlignment" Value="Stretch" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBox}">
          <Grid>
            <Border Margin="8" Background="{DynamicResource Menu.MenuItem.Static.Background}" CornerRadius="6">
              <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="{DynamicResource DropShadowColor}" Opacity="0.2" ShadowDepth="1" />
              </Border.Effect>
            </Border>
            <Border Margin="7" BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" CornerRadius="6" x:Name="Bd">
              <ScrollViewer Focusable="false" Padding="{TemplateBinding Control.Padding}" Background="#00FFFFFF">
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
              </ScrollViewer>
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="AutocompleteListBoxItemStyle" TargetType="{x:Type ListBoxItem}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
    <Setter Property="Control.VerticalContentAlignment" Value="Stretch" />
    <Setter Property="Control.HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Menu.MenuItem.Static.Foreground}" />
    <Setter Property="Panel.Background" Value="{DynamicResource Menu.MenuItem.Static.Background}" />
    <Setter Property="Border.BorderThickness" Value="0" />
    <Setter Property="Control.Padding" Value="4,2,4,2" />
    <Setter Property="FrameworkElement.Height" Value="21" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <ControlTemplate.Resources>
            <ResourceDictionary>
              <DataTemplate DataType="{x:Type controls:AutoCompleteSuggestion}" x:Key="{DataTemplateKey {x:Type controls:AutoCompleteSuggestion}}">
                <TextBlock FontSize="13" TextTrimming="CharacterEllipsis">
                  <TextBlock.Text>
                    <Binding Path="Suggestion" />
                  </TextBlock.Text>
                </TextBlock>
              </DataTemplate>
              <DataTemplate DataType="{x:Type controls:UserIdentityAutoCompleteSuggestion}" x:Key="{DataTemplateKey {x:Type controls:UserIdentityAutoCompleteSuggestion}}">
                <DockPanel>
                  <controls:AvatarImage Margin="0" DockPanel.Dock="Left" VerticalAlignment="Center" Height="14" Width="14" x:Name="AuthorAvatarImage">
                    <controls:AvatarImage.UserIdentity>
                      <Binding Path="UserIdentity" />
                    </controls:AvatarImage.UserIdentity>
                  </controls:AvatarImage>
                  <TextBlock FontSize="13" Margin="4,0,0,0" TextTrimming="CharacterEllipsis">
                    <TextBlock.Text>
                      <Binding Path="Suggestion" />
                    </TextBlock.Text>
                  </TextBlock>
                </DockPanel>
              </DataTemplate>
            </ResourceDictionary>
          </ControlTemplate.Resources>
          <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" Style="{DynamicResource ListItemRoundedBorderStyle}" x:Name="Bd">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="ListBoxItem.IsSelected" Value="True">
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Background}" Property="Panel.Background" />
              <Setter TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Border}" Property="Border.BorderBrush" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="DiffListListBoxItemStyle" TargetType="{x:Type ListBoxItem}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="Panel.Background" Value="{DynamicResource BackgroundBrush}" />
    <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <Border BorderBrush="#00FFFFFF" BorderThickness="0" Background="{TemplateBinding Panel.Background}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true" Style="{DynamicResource ListItemRoundedBorderStyle}" x:Name="Bd">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="GridViewColumnHeaderGripper" TargetType="{x:Type Thumb}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Canvas.Right" Value="-9" />
    <Setter Property="FrameworkElement.Width" Value="18" />
    <Setter Property="FrameworkElement.Height">
      <Setter.Value>
        <Binding Path="ActualHeight"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
      </Setter.Value>
    </Setter>
    <Setter Property="Control.Padding" Value="0" />
    <Setter Property="Panel.Background" Value="{DynamicResource GridViewColumnHeaderBorderBackground}" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Border Background="#00FFFFFF" Padding="{TemplateBinding Control.Padding}">
            <Rectangle Fill="{TemplateBinding Panel.Background}" HorizontalAlignment="Center" Width="1" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type GridViewColumnHeader}" TargetType="{x:Type GridViewColumnHeader}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Control.HorizontalContentAlignment" Value="Left" />
    <Setter Property="Control.VerticalContentAlignment" Value="Center" />
    <Setter Property="Control.Padding" Value="4,0,4,0" />
    <Setter Property="Panel.Background" Value="{DynamicResource GridViewColumnHeaderBackground}" />
    <Setter Property="Border.BorderBrush" Value="{DynamicResource GridViewColumnHeaderBorderBackground}" />
    <Setter Property="Border.BorderThickness" Value="0,0,0,1" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource ForegroundBrush}" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
          <Grid SnapsToDevicePixels="true">
            <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" x:Name="HeaderBorder">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition MaxHeight="7" />
                  <RowDefinition />
                </Grid.RowDefinitions>
                <Border Padding="{TemplateBinding Control.Padding}" Grid.RowSpan="2">
                  <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" Margin="0,0,0,1" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" x:Name="HeaderContent" />
                </Border>
              </Grid>
            </Border>
            <Canvas>
              <Thumb Style="{DynamicResource GridViewColumnHeaderGripper}" x:Name="PART_HeaderGripper" />
            </Canvas>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="true">
              <Setter TargetName="HeaderBorder" Value="{DynamicResource GridViewColumnHeaderHoverBackground}" Property="Panel.Background" />
            </Trigger>
            <Trigger Property="ButtonBase.IsPressed" Value="true">
              <Setter TargetName="HeaderBorder" Value="{DynamicResource GridViewColumnHeaderPressBackground}" Property="Panel.Background" />
            </Trigger>
            <Trigger Property="FrameworkElement.Height" Value="Auto">
              <Setter Property="FrameworkElement.MinHeight" Value="20" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ReferencePanelStyle" TargetType="{x:Type controls:ReferencePanel}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <x:Null />
      </Setter.Value>
    </Setter>
    <Setter Property="ItemsControl.ItemTemplate">
      <Setter.Value>
        <DataTemplate>
          <DataTemplate.Resources>
            <ResourceDictionary>
              <DataTemplate DataType="{x:Type controls:ReferencePanelTagViewModel}" x:Key="{DataTemplateKey {x:Type controls:ReferencePanelTagViewModel}}">
                <Border Height="18" BorderThickness="1" CornerRadius="4" Padding="0" Margin="0,2,4,2" BorderBrush="{DynamicResource RevisionSummary.ReferenceBorder}" Background="#00FFFFFF">
                  <DockPanel>
                    <Image Source="{DynamicResource TagIcon}" DockPanel.Dock="Left" Height="14" Width="14" Margin="2,0,2,0" />
                    <Border BorderThickness="1,0,0,0" CornerRadius="0,4,4,0" Padding="2,0" BorderBrush="{DynamicResource RevisionSummary.ReferenceBorder}" Background="{DynamicResource RevisionSummary.ReferenceBackground}">
                      <TextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                        <TextBlock.Text>
                          <Binding Path="Name" />
                        </TextBlock.Text>
                      </TextBlock>
                    </Border>
                  </DockPanel>
                </Border>
              </DataTemplate>
              <DataTemplate DataType="{x:Type controls:ReferencePanelLocalBranchViewModel}" x:Key="{DataTemplateKey {x:Type controls:ReferencePanelLocalBranchViewModel}}">
                <Border Height="18" BorderThickness="1" CornerRadius="4" Margin="0,2,4,2" Padding="2,0" BorderBrush="{DynamicResource RevisionSummary.ReferenceBorder}" Background="{DynamicResource RevisionSummary.ReferenceBackground}">
                  <TextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                    <TextBlock.Text>
                      <Binding Path="Name" />
                    </TextBlock.Text>
                  </TextBlock>
                </Border>
              </DataTemplate>
              <DataTemplate DataType="{x:Type controls:ReferencePanelRemoteBranchViewModel}" x:Key="{DataTemplateKey {x:Type controls:ReferencePanelRemoteBranchViewModel}}">
                <Border Height="18" BorderThickness="1" CornerRadius="4" Padding="0" Margin="0,2,4,2" BorderBrush="{DynamicResource RevisionSummary.ReferenceBorder}" Background="#00FFFFFF">
                  <DockPanel>
                    <Image DockPanel.Dock="Left" Height="14" Width="14" Margin="2,0,2,0">
                      <Image.Source>
                        <Binding Path="RemoteIcon" />
                      </Image.Source>
                    </Image>
                    <Border BorderThickness="1,0,0,0" CornerRadius="0,4,4,0" Padding="2,0" BorderBrush="{DynamicResource RevisionSummary.ReferenceBorder}" Background="{DynamicResource RevisionSummary.ReferenceBackground}">
                      <TextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                        <TextBlock.Text>
                          <Binding Path="Name" />
                        </TextBlock.Text>
                      </TextBlock>
                    </Border>
                  </DockPanel>
                </Border>
              </DataTemplate>
              <DataTemplate DataType="{x:Type controls:ReferencePanelBisectMarkViewModel}" x:Key="{DataTemplateKey {x:Type controls:ReferencePanelBisectMarkViewModel}}">
                <Border Height="18" BorderThickness="1" CornerRadius="4" Margin="0,2,4,2" Padding="2,0" BorderBrush="{DynamicResource RevisionSummary.ReferenceBorder}" Background="{DynamicResource RevisionSummary.ReferenceBackground}">
                  <TextBlock Style="{DynamicResource ReferenceLabelTextBlockStyle}">
                    <TextBlock.Text>
                      <Binding Path="Name" />
                    </TextBlock.Text>
                  </TextBlock>
                </Border>
              </DataTemplate>
            </ResourceDictionary>
          </DataTemplate.Resources>
          <ContentPresenter>
            <ContentPresenter.Content>
              <Binding />
            </ContentPresenter.Content>
          </ContentPresenter>
        </DataTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="ItemsControl.ItemsPanel">
      <Setter.Value>
        <ItemsPanelTemplate>
          <StackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="MultilineReferencePanelStyle" TargetType="{x:Type controls:ReferencePanel}" BasedOn="{StaticResource ReferencePanelStyle}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="ItemsControl.ItemsPanel">
      <Setter.Value>
        <ItemsPanelTemplate>
          <WrapPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>