<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:controls="clr-namespace:ForkPlus.UI.Controls" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Geometry x:Key="ArrowTop" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">M5.551,1.229L8.818,6.791C9.234,7.831 8.956,8.44 7.788,8.5L4.5,8.5L1.212,8.5C0.044,8.44 -0.234,7.831 0.182,6.791L3.449,1.229C3.8,0.759 4.15,0.512 4.5,0.5L4.5,0.5C4.85,0.512 5.2,0.758 5.551,1.229Z</Geometry>
  <Geometry x:Key="ArrowBottom" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">M0.182,2.209L3.449,7.771C3.8,8.242 4.15,8.488 4.5,8.5L4.5,8.5C4.85,8.488 5.2,8.242 5.551,7.771L8.818,2.209C9.234,1.169 8.956,0.56 7.788,0.5L1.212,0.5C0.044,0.56 -0.234,1.169 0.182,2.209Z</Geometry>
  <Geometry x:Key="ArrowRight" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">M7.771,3.449L2.209,0.182C1.169,-0.234 0.56,0.044 0.5,1.212L0.5,7.788C0.56,8.956 1.169,9.234 2.209,8.818L7.771,5.551C8.242,5.201 8.488,4.85 8.5,4.5L8.5,4.5C8.488,4.15 8.242,3.8 7.771,3.449Z</Geometry>
  <Geometry x:Key="ArrowLeft" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">M1.229,3.449L6.791,0.182C7.831,-0.234 8.44,0.044 8.5,1.212L8.5,7.788C8.44,8.956 7.831,9.234 6.791,8.818L1.229,5.551C0.758,5.201 0.512,4.85 0.5,4.5L0.5,4.5C0.512,4.15 0.758,3.8 1.229,3.449Z</Geometry>
  <Style x:Key="ScrollBarButton" TargetType="{x:Type RepeatButton}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.FocusVisualStyle" Value="{DynamicResource FocusVisual}" />
    <Setter Property="Control.HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Control.VerticalContentAlignment" Value="Stretch" />
    <Setter Property="Control.Padding" Value="1" />
    <Setter Property="UIElement.Focusable" Value="false" />
    <Setter Property="KeyboardNavigation.IsTabStop" Value="false" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Border BorderBrush="#00FFFFFF" Background="#00FFFFFF" BorderThickness="0" SnapsToDevicePixels="true" x:Name="border">
            <ContentPresenter Focusable="False" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" Margin="{TemplateBinding Control.Padding}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" x:Name="contentPresenter" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true" />
    <Setter Property="Panel.Background" Value="#00FFFFFF" />
    <Setter Property="UIElement.Focusable" Value="false" />
    <Setter Property="KeyboardNavigation.IsTabStop" Value="false" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Rectangle Fill="{TemplateBinding Panel.Background}" Height="{TemplateBinding FrameworkElement.Height}" Width="{TemplateBinding FrameworkElement.Width}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true" />
    <Setter Property="KeyboardNavigation.IsTabStop" Value="false" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid Background="#00FFFFFF">
            <Border BorderThickness="1" BorderBrush="{DynamicResource ScrollBar.Static.Thumb.Border}" Background="{DynamicResource ScrollBar.Static.Thumb}" Height="{TemplateBinding FrameworkElement.Height}" SnapsToDevicePixels="True" Width="{TemplateBinding FrameworkElement.Width}" Margin="2,0,2,0" CornerRadius="4" x:Name="rectangle" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="true">
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.MouseOver.Thumb}" Property="Panel.Background" />
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.MouseOver.Thumb.Border}" Property="Border.BorderBrush" />
            </Trigger>
            <Trigger Property="Thumb.IsDragging" Value="true">
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.Pressed.Thumb}" Property="Panel.Background" />
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.Pressed.Thumb.Border}" Property="Border.BorderBrush" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="ScrollBarThumbHorizontal" TargetType="{x:Type Thumb}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true" />
    <Setter Property="KeyboardNavigation.IsTabStop" Value="false" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid Background="#00FFFFFF">
            <Border BorderThickness="1" BorderBrush="{DynamicResource ScrollBar.Static.Thumb.Border}" Background="{DynamicResource ScrollBar.Static.Thumb.Horizontal}" Height="{TemplateBinding FrameworkElement.Height}" SnapsToDevicePixels="True" Width="{TemplateBinding FrameworkElement.Width}" Margin="0,2,0,2" CornerRadius="4" x:Name="rectangle" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="true">
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.MouseOver.Thumb.Horizontal}" Property="Panel.Background" />
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.MouseOver.Thumb.Border}" Property="Border.BorderBrush" />
            </Trigger>
            <Trigger Property="Thumb.IsDragging" Value="true">
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.Pressed.Thumb.Horizontal}" Property="Panel.Background" />
              <Setter TargetName="rectangle" Value="{DynamicResource ScrollBar.Pressed.Thumb.Border}" Property="Border.BorderBrush" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="VerticalScrollBarStyle" TargetType="{x:Type ScrollBar}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
    <Setter Property="Panel.Background" Value="{DynamicResource ScrollBar.Static.Background}" />
    <Setter Property="Border.BorderBrush" Value="{DynamicResource ScrollBar.Static.Border}" />
    <Setter Property="Border.BorderThickness" Value="1" />
    <Setter Property="FrameworkElement.Width" Value="13" />
    <Setter Property="FrameworkElement.MinWidth" Value="13" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollBar}">
          <Grid SnapsToDevicePixels="true" x:Name="Bg">
            <Grid.RowDefinitions>
              <RowDefinition Height="10" />
              <RowDefinition Height="0.00001*" />
              <RowDefinition Height="10" />
            </Grid.RowDefinitions>
            <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Grid.Row="1" />
            <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineUpButton">
              <Path Data="{DynamicResource ArrowTop}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowTop" />
            </RepeatButton>
            <Track IsDirectionReversed="true" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Grid.Row="1" Margin="0,0,0,0" x:Name="PART_Track">
              <Track.Resources>
                <ResourceDictionary>
                  <sys:Double x:Key="{x:StaticExtension SystemParameters.VerticalScrollBarButtonHeightKey}" xmlns:sys="clr-namespace:System;assembly=mscorlib">60</sys:Double>
                </ResourceDictionary>
              </Track.Resources>
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
              </Track.DecreaseRepeatButton>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
              </Track.IncreaseRepeatButton>
              <Track.Thumb>
                <Thumb Style="{DynamicResource ScrollBarThumbVertical}" />
              </Track.Thumb>
            </Track>
            <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Grid.Row="2" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineDownButton">
              <Path Data="{DynamicResource ArrowBottom}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowBottom" />
            </RepeatButton>
          </Grid>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineDownButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineDownButton" Property="IsPressed" Value="true" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowBottom" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineUpButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineUpButton" Property="IsPressed" Value="true" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowTop" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineDownButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineDownButton" Property="IsPressed" Value="false" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowBottom" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineUpButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineUpButton" Property="IsPressed" Value="false" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowTop" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <Trigger Property="UIElement.IsEnabled" Value="false">
              <Setter TargetName="ArrowTop" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
              <Setter TargetName="ArrowBottom" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="ScrollBar.Orientation" Value="Horizontal">
        <Setter Property="FrameworkElement.Width" Value="Auto" />
        <Setter Property="FrameworkElement.MinWidth" Value="0" />
        <Setter Property="FrameworkElement.Height" Value="13" />
        <Setter Property="FrameworkElement.MinHeight" Value="13" />
        <Setter Property="Border.BorderThickness" Value="1" />
        <Setter Property="Control.Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ScrollBar}">
              <Grid SnapsToDevicePixels="true" x:Name="Bg">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="10" />
                  <ColumnDefinition Width="0.00001*" />
                  <ColumnDefinition Width="10" />
                </Grid.ColumnDefinitions>
                <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Grid.Column="1" />
                <RepeatButton Command="{x:Static ScrollBar.LineLeftCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineLeftButton">
                  <Path Data="{DynamicResource ArrowLeft}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowLeft" />
                </RepeatButton>
                <Track Grid.Column="1" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Margin="0,0,0,0" x:Name="PART_Track">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
                  </Track.DecreaseRepeatButton>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
                  </Track.IncreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb Style="{DynamicResource ScrollBarThumbHorizontal}" />
                  </Track.Thumb>
                </Track>
                <RepeatButton Grid.Column="2" Command="{x:Static ScrollBar.LineRightCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineRightButton">
                  <Path Data="{DynamicResource ArrowRight}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowRight" />
                </RepeatButton>
              </Grid>
              <ControlTemplate.Triggers>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineRightButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineRightButton" Property="IsPressed" Value="true" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowRight" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineLeftButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineLeftButton" Property="IsPressed" Value="true" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowLeft" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineRightButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineRightButton" Property="IsPressed" Value="false" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowRight" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineLeftButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineLeftButton" Property="IsPressed" Value="false" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowLeft" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <Trigger Property="UIElement.IsEnabled" Value="false">
                  <Setter TargetName="ArrowLeft" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
                  <Setter TargetName="ArrowRight" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="HorizontalScrollBarStyle" TargetType="{x:Type ScrollBar}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
    <Setter Property="Panel.Background" Value="{DynamicResource ScrollBar.Static.Background}" />
    <Setter Property="Border.BorderBrush" Value="{DynamicResource ScrollBar.Static.Border}" />
    <Setter Property="Border.BorderThickness" Value="1" />
    <Setter Property="FrameworkElement.Width" Value="13" />
    <Setter Property="FrameworkElement.MinWidth" Value="13" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollBar}">
          <Grid SnapsToDevicePixels="true" x:Name="Bg">
            <Grid.RowDefinitions>
              <RowDefinition Height="10" />
              <RowDefinition Height="0.00001*" />
              <RowDefinition Height="10" />
            </Grid.RowDefinitions>
            <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Grid.Row="1" />
            <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineUpButton">
              <Path Data="{DynamicResource ArrowTop}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowTop" />
            </RepeatButton>
            <Track IsDirectionReversed="true" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Grid.Row="1" Margin="0,0,0,0" x:Name="PART_Track">
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
              </Track.DecreaseRepeatButton>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
              </Track.IncreaseRepeatButton>
              <Track.Thumb>
                <Thumb Style="{DynamicResource ScrollBarThumbVertical}" />
              </Track.Thumb>
            </Track>
            <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Grid.Row="2" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineDownButton">
              <Path Data="{DynamicResource ArrowBottom}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowBottom" />
            </RepeatButton>
          </Grid>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineDownButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineDownButton" Property="IsPressed" Value="true" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowBottom" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineUpButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineUpButton" Property="IsPressed" Value="true" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowTop" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineDownButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineDownButton" Property="IsPressed" Value="false" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowBottom" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition SourceName="PART_LineUpButton" Property="IsMouseOver" Value="true" />
                <Condition SourceName="PART_LineUpButton" Property="IsPressed" Value="false" />
              </MultiTrigger.Conditions>
              <Setter TargetName="ArrowTop" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
            </MultiTrigger>
            <Trigger Property="UIElement.IsEnabled" Value="false">
              <Setter TargetName="ArrowTop" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
              <Setter TargetName="ArrowBottom" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="ScrollBar.Orientation" Value="Horizontal">
        <Setter Property="FrameworkElement.Width" Value="Auto" />
        <Setter Property="FrameworkElement.MinWidth" Value="0" />
        <Setter Property="FrameworkElement.Height" Value="13" />
        <Setter Property="FrameworkElement.MinHeight" Value="13" />
        <Setter Property="Border.BorderThickness" Value="1" />
        <Setter Property="Control.Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ScrollBar}">
              <Grid SnapsToDevicePixels="true" x:Name="Bg">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="10" />
                  <ColumnDefinition Width="0.00001*" />
                  <ColumnDefinition Width="10" />
                </Grid.ColumnDefinitions>
                <Border BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="{TemplateBinding Border.BorderThickness}" Background="{TemplateBinding Panel.Background}" Grid.Column="1" />
                <RepeatButton Command="{x:Static ScrollBar.LineLeftCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineLeftButton">
                  <Path Data="{DynamicResource ArrowLeft}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowLeft" />
                </RepeatButton>
                <Track Grid.Column="1" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Margin="0,0,0,0" x:Name="PART_Track">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
                  </Track.DecreaseRepeatButton>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{DynamicResource RepeatButtonTransparent}" />
                  </Track.IncreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb Style="{DynamicResource ScrollBarThumbHorizontal}" />
                  </Track.Thumb>
                </Track>
                <RepeatButton Grid.Column="2" Command="{x:Static ScrollBar.LineRightCommand}" IsEnabled="{TemplateBinding UIElement.IsMouseOver}" Style="{DynamicResource ScrollBarButton}" x:Name="PART_LineRightButton">
                  <Path Data="{DynamicResource ArrowRight}" Fill="{DynamicResource ScrollBar.Static.Glyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="None" x:Name="ArrowRight" />
                </RepeatButton>
              </Grid>
              <ControlTemplate.Triggers>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineRightButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineRightButton" Property="IsPressed" Value="true" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowRight" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineLeftButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineLeftButton" Property="IsPressed" Value="true" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowLeft" Value="{DynamicResource ScrollBar.Pressed.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineRightButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineRightButton" Property="IsPressed" Value="false" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowRight" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition SourceName="PART_LineLeftButton" Property="IsMouseOver" Value="true" />
                    <Condition SourceName="PART_LineLeftButton" Property="IsPressed" Value="false" />
                  </MultiTrigger.Conditions>
                  <Setter TargetName="ArrowLeft" Value="{DynamicResource ScrollBar.MouseOver.Glyph}" Property="Shape.Fill" />
                </MultiTrigger>
                <Trigger Property="UIElement.IsEnabled" Value="false">
                  <Setter TargetName="ArrowLeft" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
                  <Setter TargetName="ArrowRight" Value="{DynamicResource ScrollBar.Disabled.Glyph}" Property="Shape.Fill" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{x:Type ScrollViewer}" TargetType="{x:Type ScrollViewer}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Setter Property="Panel.Background" Value="{DynamicResource ScrollViewerBackground}" />
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollViewer}">
          <Grid Background="{TemplateBinding Panel.Background}" x:Name="Grid">
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Rectangle Grid.Row="1" Fill="{DynamicResource ScrollViewer.CornerBackgroundBrush}" Grid.Column="1" x:Name="Corner" />
            <ScrollContentPresenter Grid.Row="0" Margin="{TemplateBinding Control.Padding}" Grid.Column="0" Content="{TemplateBinding ContentControl.Content}" ContentTemplate="{x:Null}" CanVerticallyScroll="False" CanHorizontallyScroll="False" CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}" x:Name="PART_ScrollContentPresenter" />
            <ScrollBar ViewportSize="{TemplateBinding ScrollViewer.ViewportHeight}" Visibility="{TemplateBinding ScrollViewer.ComputedVerticalScrollBarVisibility}" Grid.Row="0" Minimum="0" Maximum="{TemplateBinding ScrollViewer.ScrollableHeight}" Grid.Column="1" Cursor="Arrow" AutomationProperties.AutomationId="VerticalScrollBar" Style="{DynamicResource VerticalScrollBarStyle}" x:Name="PART_VerticalScrollBar">
              <ScrollBar.Value>
                <Binding Path="VerticalOffset" Mode="OneWay"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
              </ScrollBar.Value>
            </ScrollBar>
            <ScrollBar ViewportSize="{TemplateBinding ScrollViewer.ViewportWidth}" Visibility="{TemplateBinding ScrollViewer.ComputedHorizontalScrollBarVisibility}" Grid.Row="1" Orientation="Horizontal" Minimum="0" Maximum="{TemplateBinding ScrollViewer.ScrollableWidth}" Grid.Column="0" Cursor="Arrow" AutomationProperties.AutomationId="HorizontalScrollBar" Style="{DynamicResource HorizontalScrollBarStyle}" x:Name="PART_HorizontalScrollBar">
              <ScrollBar.Value>
                <Binding Path="HorizontalOffset" Mode="OneWay"><Binding.RelativeSource><RelativeSource Mode="TemplatedParent" /></Binding.RelativeSource></Binding>
              </ScrollBar.Value>
            </ScrollBar>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type controls:TouchpadAwareScrollViewer}" TargetType="{x:Type controls:TouchpadAwareScrollViewer}" BasedOn="{StaticResource {x:Type ScrollViewer}}" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" />
</ResourceDictionary>