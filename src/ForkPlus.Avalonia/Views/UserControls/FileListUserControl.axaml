<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:uc="clr-namespace:ForkPlus.Avalonia.Views.UserControls"
             x:Class="ForkPlus.Avalonia.Views.UserControls.FileListUserControl">
    <!--
        Avalonia 版 FileListUserControl（spike 简化升级版）。

        对照 WPF 工程 src/ForkPlus/UI/UserControls/FileListUserControl.xaml（121 行）：
          - 顶层 FileListTreeView（继承 MultiselectionTreeView，自定义 TreeView）
            + VirtualizingPanel.IsVirtualizing=True + VirtualizationMode=Recycling
          - TreeView.View: GridView 2 列：
            Col "Name":    DataTemplate Grid 3 列（ChangeTypeIcon + FileTypeIcon + FileName）
            Col "Location": AutoTooltipTextBlock 绑定 FolderPath
          - Resources: 2 个 DataTemplate（TreeViewTemplate + ListViewTemplate）

        对照 WPF 工程 src/ForkPlus/UI/UserControls/FileListUserControl.xaml.cs（847 行）：
          - 4 个公共事件 + 公共属性 + 11 个公共方法
          - 增量树构建算法（ArrayDiff.Diff + ApplyAddedEntries + ApplyRemovedEntries）
          - 大列表后台构建（>= 5000 项时 Task.Run）
          - FileListMode 三模式切换（Tree/List/CombinedList）

        Avalonia 版差异：
          - WPF FileListTreeView（继承 MultiselectionTreeView）→ 原生 TreeView + TreeDataTemplate
          - WPF FileListItem : MultiselectionTreeViewItem → POCO（FileListItem.cs）
          - WPF AutoTooltipTextBlock → TextBlock（spike）
          - WPF GridView 2 列 → TreeDataTemplate 单列（spike）
          - WPF DataTrigger → 不迁移（spike 用 emoji 绑定）
          - WPF MouseDoubleClick → DoubleTapped

        spike 简化：
          - 用 TreeView 显示文件树，FileListItem POCO 绑定
          - Refresh(ChangedFile[]) 用 ObservableCollection 填充
          - SetFilter(string) 按路径过滤
          - FileSelected 事件在选择变化时触发
          - 增量树构建 / 虚拟化 / 拖拽 / 三模式切换暂不实现
    -->
    <UserControl.DataTemplates>
        <TreeDataTemplate DataType="uc:FileListItem"
                          ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding ChangeTypeEmoji}" Margin="0,0,4,0" />
                <TextBlock Text="{Binding FileName}" />
            </StackPanel>
        </TreeDataTemplate>
    </UserControl.DataTemplates>

    <TreeView Name="TreeView"
              Background="{DynamicResource ThemeBackgroundBrush}"
              BorderThickness="0"
              SelectionChanged="TreeView_SelectionChanged"
              DoubleTapped="TreeView_DoubleTapped" />
</UserControl>
